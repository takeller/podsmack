<!DOCTYPE html>
<html>
  <head>
    <title>Podsmack</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all' %>
    <%= javascript_pack_tag 'application' %>

  </head>


  <body id="page-top" class="background-image"><div id="huntr-react-container-2"><!-- react-empty: 1 --></div>

    <nav class="navbar navbar-expand-lg navbar-light fixed-top" id="mainNav">
      <div class="container">
        <a class="navbar-brand js-scroll-trigger" href="#page-top">Podsmack</a>
        <button class="navbar-toggler navbar-toggler-right collapsed" type="button" data-toggle="collapse" data-target="#navbarResponsive" aria-controls="navbarResponsive" aria-expanded="false" aria-label="Toggle navigation">
          Menu
          <i class="fas fa-bars"></i>
        </button>
        <div class="collapse navbar-collapse" id="navbarResponsive">
          <ul class="navbar-nav ml-auto">
            <li class="nav-item">
              <% if !current_user.nil? %>
              <a class="nav-link js-scroll-trigger" href="#features">Dashboard</a>
              <% end %>
            </li>
            <li class="nav-item">
              <a class="nav-link js-scroll-trigger" href="#contact">Browse</a>
            </li>
            <li class="nav-item">
              <% if current_user.nil? %>
                <a class="nav-link js-scroll-trigger" href="/login">Login/Register</a>
              <% end %>
            </li>
            <li class="nav-item">
              <% if current_user %>
                <a class="nav-link js-scroll-trigger" href="/logout">Logout</a>
               <% end %>
            </li>
          </ul>
        </div>
      </div>
    </nav>

  <body>
  <% flash.each do |name, msg| %>
    <div class= "<%=name%>-flash">
      <p><%= msg %></p>
    </div>
  <% end %>
  <%= yield %>

  </body>

</html>
